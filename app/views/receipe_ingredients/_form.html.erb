<%= form_for(@receipe_ingredient) do |f| %>
  <% if @receipe_ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@receipe_ingredient.errors.count, "error") %> prohibited this receipe_ingredient from being saved:</h2>

      <ul>
      <% @receipe_ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :recipe_id %><br>
    <%= f.text_field :recipe_id %>
  </div>
  <div class="field">
    <%= f.label :ingredient_id %><br>
    <%= f.text_field :ingredient_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
