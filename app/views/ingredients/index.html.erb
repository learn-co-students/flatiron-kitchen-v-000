<h1>All ingredients</h1>
<%= link_to "New ingredient", new_ingredient_path %>
<ul>
  <% @ingredients.each do |ing| %>
    <li>
      <%= ing.name %> - <%= link_to "edit", edit_ingredient_path(ing) %>
    </li>
  <% end %>
</ul>
