<h1>List of Ingredients Stocked in Our Kitchen</h1>

<ul>
  <% @ingredients.each do |ingredient| %>
    <li><%= ingredient.name.split.map(&:capitalize).join(' ') %></li>
  <% end %>
</ul>

<p><%= link_to "Add Ingredient", new_ingredient_path(@ingredient) %></p>
