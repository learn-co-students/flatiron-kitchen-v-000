<h4><%= ingredient.name %>, Quantity: <%= ingredient.quantity %></h4>
<p>Appears in the following recipes: 
<ul class="comma-list">
  <% ingredient.recipes.each do |recipe| %> 
    <li><%= link_to recipe.name, recipe_path(recipe) %></li>
  <% end %> 
</ul>
</p>