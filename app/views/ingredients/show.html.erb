<h2>Ingredient: <%= @ingredient.name %></h2>
<h2>Inventory Total: <%= @ingredient.inventory_total %></h2>

<h3>Belongs to the following recipes:</h3>
<ul>
  <% @ingredient.recipes.each do |recipe| %>
    <li><%= link_to recipe.name, recipe %></li>
  <% end %>
</ul>

<%= link_to 'Edit Ingredient', edit_ingredient_path %>
