<h1>Recipes</h1>
<%= link_to 'Create a new Recipe', new_recipe_path %>

<ul>
  <% @recipes.each do |recipe| %>
    <li><%= render partial: 'recipe', locals: {recipe: recipe} %></li>
  <% end %>
</ul>
