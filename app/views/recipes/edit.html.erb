<h3> Edit a Recipe </h3>
  <%= form_for(@recipe) do |f| %>
  <%= render partial: 'form', locals: {f: f} %>
    <%= f.submit %>
  <br></br>
  <%= render partial: 'add_ingredients', locals: {f: f} %>
<% end %>
    <br></br>
