<h1>Edit Recipe</h1>

<%= form_for @recipe, id: 'edit_recipe' do |f| %>

<%= render partial: "ingredients/form", locals: {f: f} %>

<%= f.collection_check_boxes(:ingredient_ids, Ingredient.all, :id, :name) %>

<%= f.submit %>

<% end %>
