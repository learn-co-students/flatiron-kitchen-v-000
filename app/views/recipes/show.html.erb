<h1><%= @recipe.name %></h1>

<h3>Ingredients: </h3>
<ul>
  <% @recipe.ingredients.each do |ingredient| %>
    <li><%= link_to ingredient.name, ingredient_path(ingredient) %></li>
  <% end %>
</ul>

<h4><%= link_to "Edit #{@recipe.name}", edit_recipe_path(@recipe) %></h4>
