Create A New Recipe
<%= form_for @recipe do |f| %>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.submit %>
<% end %>

<%= collection_check_boxes(:recipe, :ingredient_ids, Ingredient.all, :id, :name) %>