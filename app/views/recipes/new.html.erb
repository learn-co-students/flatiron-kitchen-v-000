<%= form_for @recipe, {:id => "new_recipe" } do  |f| %>
  <%= f.text_field :name %>
<ol>
  <%@ingredients.each do |i| %>
  		<li><label for="<%=i.id%>"><input id="<%=i.id%>" type="checkbox" name="recipe[ingredient_ids][]" value="<%= i.name %>"><%= i.name %> </label></li>
  
  <%end%>
  </ol>
  <br>
  <%= f.submit %>
<% end %>
<br>
<br>

   Failure/Error: expect(page).to have_css("input[type=\"checkbox\"]", :count => 4)
       expected to find css "input[type=\"checkbox\"]" 4 times but there were no matches
