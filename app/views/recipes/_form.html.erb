<%= form_for recipe do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  <br></br>

  <div class="field">
    <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %><br></br>
  </div>

  <%= f.submit %>
<% end %>