<%= form_for recipe do |r| %>
  <%= r.label :name %>
  <%= r.text_field :name %>

  <br></br>
  <%= r.collection_check_boxes :ingredients, Ingredient.all, :id, :name %>

  <br></br>
  <%= r.submit %>
<% end %>