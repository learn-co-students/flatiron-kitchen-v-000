<%= form_for recipe do |f|%>

            <%= f.label :name %><br>
            <%= f.text_field :name %><br>
            <%= f.collection_check_boxes :ingredient_ids, Ingredient.all.order(:name), :id, :name%>  <br>
            <%= f.submit%>

<% end %>
