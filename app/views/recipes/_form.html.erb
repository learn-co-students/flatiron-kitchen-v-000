<%= form_for instance_var do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %> <br></br>

  <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %><br></br>

  <%= f.submit %>
<% end %>
