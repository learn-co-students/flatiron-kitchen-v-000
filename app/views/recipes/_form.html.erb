<%= form_for recipe do |f|%>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <div class="field">
    <%= f.collection_check_boxes :ingredient_ids, Ingredient.all, :id, :name %><br>
  </div>

  <%= f.submit %>
<% end %>
