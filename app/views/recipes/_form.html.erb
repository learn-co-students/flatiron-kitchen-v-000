<%= form_for recipe do |f|%>
  <p><%= f.label :name %>
  <%= f.text_field :name %></p>

<!--should display all the existing ingredients-->
<%= f.collection_check_boxes(:recipe_ingredient_ids, Ingredient.all, :id, :name) %>

<%= f.submit %>
<% end %>
