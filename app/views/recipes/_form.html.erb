<%= form_for recipe do |f|%>
<p>
  <%= f.label :name %>
  <%= f.text_field :name %>
</p>
<p>
  <%= f.collection_check_boxes :ingredient_ids, @ingredients, :id, :name %><br>
</p>
  <%= f.submit %>
<% end %>
