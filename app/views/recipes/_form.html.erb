<%= form_for recipe do |r| %>
<%= r.label :name%>
<%= r.text_field :name %>
<br><br>
<%= r.label :ingredients %> <br>
<%= r.collection_check_boxes(:ingredient_ids, Ingredient.all, :id, :name) do |i|%>
  <%= i.check_box %> <%= i.label %> <br>
<% end %>
<br><br>
<%= r.submit %>
<% end %>
